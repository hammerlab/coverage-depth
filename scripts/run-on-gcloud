#!/usr/bin/env bash

export JAR=gs://hammerlab-lib/pageant-1ca9ed2.jar
export MAIN=org.hammerlab.pageant.coverage.CoverageDepth
if [ -z "$CLUSTER" ]; then
	export CLUSTER=pageant
fi
"$(dirname "${BASH_SOURCE[0]}")"/../dataproc/dataproc "$@"
